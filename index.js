var render = {
  /**
   *
   * @param conf
   * {
   *   len: 10, // 长度
   *   upper: true, // 必须包含大写
   *   num: true,   // 必须包含数字
   *   letter: true,  // 必须包含字母
   *   reg: '(\w)\1(\w)\1\1'    //
   * }
   */
  create: function(conf){

  }
};

module.exports = render;